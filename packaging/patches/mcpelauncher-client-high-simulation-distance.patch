From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Abid speedcuber <abid-speedcuber@users.noreply.github.com>
Date: Sat, 6 Jun 2026 12:50:00 +0600
Subject: [PATCH] Force high simulation distance options


---
diff --git a/mcpelauncher-client/src/main.cpp b/mcpelauncher-client/src/main.cpp
index 9a678f0..2e82a42 100644
--- a/mcpelauncher-client/src/main.cpp
+++ b/mcpelauncher-client/src/main.cpp
@@ -43,6 +43,8 @@
 #include <fstream>
 #include "glad/glad.h"
 // For getpid
+#include <cstring>
+#include <sys/mman.h>
 #include <unistd.h>
 #include <simpleipc/client/service_client.h>
 #include <daemon_utils/auto_shutdown_service.h>
@@ -135,6 +137,7 @@ static bool optionExists(const char* key) {
 void readOptions();
 void applyHighDistanceOptions();
 void disableChromebookDetection(void* handle);
+void forceHighSimulationDistance(void* handle);
 
 int main(int argc, char* argv[]) {
     if(argc == 2 && argv[1][0] != '-') {
@@ -560,6 +563,7 @@ Hardware	: Qualcomm Technologies, Inc MSM8998
     Log::debug("Launcher", "Minecraft is at offset 0x%" PRIXPTR, (uintptr_t)MinecraftUtils::getLibraryBase(handle));
     base = MinecraftUtils::getLibraryBase(handle);
     disableChromebookDetection(handle);
+    forceHighSimulationDistance(handle);
 
     if(!freeOnly.get()) {
         modLoader.loadModsFromDirectory(PathHelper::getPrimaryDataDirectory() + "mods/");
@@ -783,6 +787,183 @@ void disableChromebookDetection(void* handle) {
 #endif
 }
 
+static bool makeMinecraftMemoryWritable(uintptr_t address, size_t size, int protection) {
+#if defined(__i386__) || defined(__x86_64__)
+    if(size == 0 || address + size < address)
+        return false;
+
+    long pageSize = sysconf(_SC_PAGESIZE);
+    if(pageSize <= 0)
+        return false;
+
+    auto page = address & ~(static_cast<uintptr_t>(pageSize) - 1);
+    auto end = address + size;
+    if(end + static_cast<uintptr_t>(pageSize) - 1 < end)
+        return false;
+    auto pageEnd = (end + static_cast<uintptr_t>(pageSize) - 1) & ~(static_cast<uintptr_t>(pageSize) - 1);
+    return mprotect(reinterpret_cast<void*>(page), pageEnd - page, protection) == 0;
+#else
+    (void)address;
+    (void)size;
+    (void)protection;
+    return false;
+#endif
+}
+
+static bool isMinecraftMemoryReadable(uintptr_t address, size_t size) {
+#if defined(__i386__) || defined(__x86_64__)
+    if(size == 0 || address + size < address)
+        return false;
+
+    std::ifstream maps("/proc/self/maps");
+    std::string line;
+    while(std::getline(maps, line)) {
+        auto dash = line.find('-');
+        auto space = line.find(' ', dash + 1);
+        if(dash == std::string::npos || space == std::string::npos || space + 1 >= line.size())
+            continue;
+
+        try {
+            auto start = static_cast<uintptr_t>(std::stoull(line.substr(0, dash), nullptr, 16));
+            auto end = static_cast<uintptr_t>(std::stoull(line.substr(dash + 1, space - dash - 1), nullptr, 16));
+            if(line[space + 1] == 'r' && address >= start && address + size <= end)
+                return true;
+        } catch(...) {
+            continue;
+        }
+    }
+    return false;
+#else
+    (void)address;
+    (void)size;
+    return false;
+#endif
+}
+
+static bool patchMinecraftBytes(void* handle, uintptr_t offset, const unsigned char* expected, const unsigned char* replacement, size_t size, const char* name) {
+#if defined(__i386__) || defined(__x86_64__)
+    auto base = reinterpret_cast<uintptr_t>(MinecraftUtils::getLibraryBase(handle));
+    auto address = base + offset;
+    if(address < base || !isMinecraftMemoryReadable(address, size)) {
+        Log::warn("Launcher", "Skipped %s patch: target address 0x%zx is not mapped/readable", name, static_cast<size_t>(offset));
+        return false;
+    }
+
+    if(std::memcmp(reinterpret_cast<void*>(address), expected, size) != 0) {
+        Log::warn("Launcher", "Skipped %s patch: byte pattern mismatch at 0x%zx", name, static_cast<size_t>(offset));
+        return false;
+    }
+
+    if(!makeMinecraftMemoryWritable(address, size, PROT_READ | PROT_WRITE | PROT_EXEC)) {
+        Log::warn("Launcher", "Failed to make Minecraft code writable for %s patch", name);
+        return false;
+    }
+
+    std::memcpy(reinterpret_cast<void*>(address), replacement, size);
+    __builtin___clear_cache(reinterpret_cast<char*>(address), reinterpret_cast<char*>(address + size));
+    makeMinecraftMemoryWritable(address, size, PROT_READ | PROT_EXEC);
+    Log::info("Launcher", "Applied %s patch at 0x%zx", name, static_cast<size_t>(offset));
+    return true;
+#else
+    (void)handle;
+    (void)offset;
+    (void)expected;
+    (void)replacement;
+    (void)size;
+    (void)name;
+    return false;
+#endif
+}
+
+static bool patchUniqueMinecraftSignature(void* handle, const unsigned char* signature, size_t signatureSize,
+                                          size_t patchOffset, const unsigned char* expected,
+                                          const unsigned char* replacement, size_t patchSize, const char* name) {
+#if defined(__i386__) || defined(__x86_64__)
+    auto base = reinterpret_cast<uintptr_t>(MinecraftUtils::getLibraryBase(handle));
+    uintptr_t match = 0;
+    size_t matchCount = 0;
+    std::ifstream maps("/proc/self/maps");
+    std::string line;
+    while(std::getline(maps, line)) {
+        auto dash = line.find('-');
+        auto space = line.find(' ', dash + 1);
+        if(dash == std::string::npos || space == std::string::npos || space + 4 >= line.size() ||
+           line[space + 3] != 'x' || line.find("libminecraftpe.so") == std::string::npos)
+            continue;
+
+        uintptr_t start;
+        uintptr_t end;
+        try {
+            start = static_cast<uintptr_t>(std::stoull(line.substr(0, dash), nullptr, 16));
+            end = static_cast<uintptr_t>(std::stoull(line.substr(dash + 1, space - dash - 1), nullptr, 16));
+        } catch(...) {
+            continue;
+        }
+        if(end <= start || signatureSize > end - start)
+            continue;
+
+        auto bytes = reinterpret_cast<const unsigned char*>(start);
+        size_t limit = static_cast<size_t>(end - start) - signatureSize;
+        for(size_t i = 0; i <= limit; ++i) {
+            if(bytes[i] == signature[0] && std::memcmp(bytes + i, signature, signatureSize) == 0) {
+                match = start + i + patchOffset;
+                ++matchCount;
+            }
+        }
+    }
+
+    if(matchCount != 1 || match < base) {
+        Log::warn("Launcher", "Skipped %s patch: signature matched %zu locations", name, matchCount);
+        return false;
+    }
+    return patchMinecraftBytes(handle, match - base, expected, replacement, patchSize, name);
+#else
+    (void)handle;
+    (void)signature;
+    (void)signatureSize;
+    (void)patchOffset;
+    (void)expected;
+    (void)replacement;
+    (void)patchSize;
+    (void)name;
+    return false;
+#endif
+}
+
+static int normalizedHighSimulationChunks() {
+    int chunks = Settings::high_distance_simulation_chunks;
+    if(chunks < 4)
+        chunks = 4;
+    if(chunks > 126)
+        chunks = 126;
+    if((chunks % 2) != 0)
+        --chunks;
+    return chunks;
+}
+
+void forceHighSimulationDistance(void* handle) {
+    if(!Settings::enable_high_distance_options || Settings::high_distance_simulation_chunks <= 8 || Settings::high_distance_simulation_chunks > 127)
+        return;
+
+    unsigned char expected[] = {0xff, 0x90, 0x70, 0x07, 0x00, 0x00};
+    unsigned char replacement[] = {0xb8, static_cast<unsigned char>(Settings::high_distance_simulation_chunks), 0x00, 0x00, 0x00, 0x90};
+    unsigned char simulationMaxSignature[] = {
+        0xff, 0x90, 0x70, 0x07, 0x00, 0x00,
+        0x48, 0x89, 0x44, 0x24, 0x48, 0x48, 0x8d, 0x84, 0x24, 0x00, 0x01, 0x00, 0x00
+    };
+    unsigned char oreUiMaxSignature[] = {
+        0xff, 0x90, 0x70, 0x07, 0x00, 0x00,
+        0x89, 0xc5, 0x0f, 0x57, 0xc0, 0x0f, 0x11, 0x44, 0x24, 0x38
+    };
+    bool patchedSimulation = patchUniqueMinecraftSignature(handle, simulationMaxSignature, sizeof(simulationMaxSignature),
+                                                            0, expected, replacement, sizeof(expected), "simulation distance max");
+    bool patchedOreUi = patchUniqueMinecraftSignature(handle, oreUiMaxSignature, sizeof(oreUiMaxSignature),
+                                                       0, expected, replacement, sizeof(expected), "OreUI simulation distance options max");
+    Log::info("Launcher", "Simulation distance patches: game=%s OreUI=%s max=%d chunks",
+              patchedSimulation ? "applied" : "skipped", patchedOreUi ? "applied" : "skipped",
+              normalizedHighSimulationChunks());
+}
+
 void applyHighDistanceOptions() {
     if(!Settings::enable_high_distance_options)
         return;
@@ -792,6 +973,7 @@ void applyHighDistanceOptions() {
     properties::property<int> particleViewDistance(properties, "gfx_particleviewdistance", 0);
     properties::property<int> rayTracingViewDistance(properties, "raytracing_viewdistance", 0);
     properties::property<int> deferredViewDistance(properties, "deferred_viewdistance", 0);
+    properties::property<int> simDistance(properties, "simulationdistance", 0);
     properties::property<int> memoryTierOverride(properties, "device_info_memory_tier_override", 0);
     properties::property<bool> useMemoryTierOverride(properties, "device_info_use_memory_tier_override", false);
     properties::property<bool> showRenderDistanceWarning(properties, "show_render_distance_warning_modal", false);
@@ -816,13 +998,15 @@ void applyHighDistanceOptions() {
     seedInt("gfx_particleviewdistance", particleViewDistance, Settings::high_distance_particle_chunks);
     seedInt("raytracing_viewdistance", rayTracingViewDistance, Settings::high_distance_raytracing_chunks);
     seedInt("deferred_viewdistance", deferredViewDistance, Settings::high_distance_deferred_chunks);
+    seedInt("simulationdistance", simDistance, Settings::high_distance_simulation_chunks);
     seedInt("device_info_memory_tier_override", memoryTierOverride, Settings::high_distance_memory_tier);
     setBool(useMemoryTierOverride, true);
     setBool(showRenderDistanceWarning, false);
 
     if(changed) {
-        Log::info("Launcher", "Applied high distance options: render=%d, particles=%d, memory tier=%d",
-                  Settings::high_distance_render_chunks, Settings::high_distance_particle_chunks, Settings::high_distance_memory_tier);
+        Log::info("Launcher", "Applied high distance options: render=%d, sim=%d, particles=%d, memory tier=%d",
+                  Settings::high_distance_render_chunks, Settings::high_distance_simulation_chunks,
+                  Settings::high_distance_particle_chunks, Settings::high_distance_memory_tier);
         saveOptions(properties);
     }
 }
diff --git a/mcpelauncher-client/src/settings.cpp b/mcpelauncher-client/src/settings.cpp
index 023cab0..29ec6b2 100644
--- a/mcpelauncher-client/src/settings.cpp
+++ b/mcpelauncher-client/src/settings.cpp
@@ -32,6 +32,7 @@ int Settings::high_distance_render_chunks = 448;
 int Settings::high_distance_particle_chunks = 256;
 int Settings::high_distance_raytracing_chunks = 64;
 int Settings::high_distance_deferred_chunks = 64;
+int Settings::high_distance_simulation_chunks = 22;
 int Settings::high_distance_memory_tier = 4;
 
 char GameOptions::leftKey = 'A';
@@ -72,6 +73,7 @@ static properties::property<int> high_distance_render_chunks(settings, "high_dis
 static properties::property<int> high_distance_particle_chunks(settings, "high_distance_particle_chunks", /* default if not defined*/ 256);
 static properties::property<int> high_distance_raytracing_chunks(settings, "high_distance_raytracing_chunks", /* default if not defined*/ 64);
 static properties::property<int> high_distance_deferred_chunks(settings, "high_distance_deferred_chunks", /* default if not defined*/ 64);
+static properties::property<int> high_distance_simulation_chunks(settings, "high_distance_simulation_chunks", /* default if not defined*/ 22);
 static properties::property<int> high_distance_memory_tier(settings, "high_distance_memory_tier", /* default if not defined*/ 4);
 
 static int clampedDistance(int value) {
@@ -121,6 +123,7 @@ void Settings::load() {
     Settings::high_distance_particle_chunks = clampedDistance(::high_distance_particle_chunks.get());
     Settings::high_distance_raytracing_chunks = clampedDistance(::high_distance_raytracing_chunks.get());
     Settings::high_distance_deferred_chunks = clampedDistance(::high_distance_deferred_chunks.get());
+    Settings::high_distance_simulation_chunks = clampedDistance(::high_distance_simulation_chunks.get());
     Settings::high_distance_memory_tier = clampedMemoryTier(::high_distance_memory_tier.get());
 }
 
@@ -155,6 +158,7 @@ void Settings::save() {
         ::high_distance_particle_chunks.set(Settings::high_distance_particle_chunks);
         ::high_distance_raytracing_chunks.set(Settings::high_distance_raytracing_chunks);
         ::high_distance_deferred_chunks.set(Settings::high_distance_deferred_chunks);
+        ::high_distance_simulation_chunks.set(Settings::high_distance_simulation_chunks);
         ::high_distance_memory_tier.set(Settings::high_distance_memory_tier);
 
         settings.save(propertiesFile);
diff --git a/mcpelauncher-client/src/settings.h b/mcpelauncher-client/src/settings.h
index 5b87fd3..980ba51 100644
--- a/mcpelauncher-client/src/settings.h
+++ b/mcpelauncher-client/src/settings.h
@@ -32,6 +32,7 @@ struct Settings {
     static int high_distance_particle_chunks;
     static int high_distance_raytracing_chunks;
     static int high_distance_deferred_chunks;
+    static int high_distance_simulation_chunks;
     static int high_distance_memory_tier;
 
     static std::string getPath();
